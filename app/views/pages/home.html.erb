<div class="img-fluid py-5" style="height:500px; background-size: cover; background-image:url('https://images.pexels.com/photos/3861951/pexels-photo-3861951.jpeg')">
  <div class="row">
    <div class="jumbotron jumbotron-fluid p-5 bg-white col-6 rounded mx-auto d-block justify-content-center align-items-center">
      <h1 class="display-4"> 🛟 e-TAide </h1>
        <p class="lead"> Sélectionnez un exercice, <strong> réservez votre TA ! </strong> </p>
        <div>
          <%= form_with url: exercises_path, method: :get, class: "d-flex" do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control form-control-lg",
            placeholder: "Chercher un exercice par nom ou catégorie"
          %>
          <%= submit_tag "Rechercher", class: "btn btn-primary btn-lg" %>
        <% end %>
        <hr>
        <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
          <div class="carousel-inner">
            <div class="carousel-item active">
              <p>A</p>
            </div>
            <div class="carousel-item">
              <p>B</p>
            </div>
            <div class="carousel-item">
              <p>C</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <%# # Integrating iteration to show sample of exercises%>
  <div class="row mx-4 p-5">
    <h2>Derniers exercices</h2>
    <%= link_to "Voir tous les exercises ⟩", exercises_path, class:"my-2 text-decoration-none" %>
    <% @exercises.limit(9).each do |exercise| %>
      <div class="col-sm-4">
        <div class="shadow-sm card p-3 mb-2">
          <h3 class="my-3"> <%= exercise.name %></h3>
          <% if exercise.category == "Front-end" %>
            <p class='tag tag-teal text-center'> <%= exercise.category %> <p>
          <% elsif exercise.category == "Back-end" %>
            <p class='tag tag-pink text-center'> <%= exercise.category %> <p>
          <% elsif exercise.category == "Back-end" %>
            <p class='tag tag-yellow text-center'> <%= exercise.category %> <p>
          <% elsif exercise.category == "Back-end" %>
            <p class='tag tag-green text-center'> <%= exercise.category %> <p>
          <% else %>
            <p class='tag tag-purple text-center'> <%= exercise.category %> <p>
          <% end %>
          <p> <strong> TA associé: </strong>  <%= exercise.user.first_name %> <%= exercise.user.last_name %></p>
            <%= link_to "Réserver pour #{exercise.price_per_day.round(0)}€", exercise_path(exercise), class:"btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>

</div>
